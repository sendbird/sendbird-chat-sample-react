/**
 * @internal
 */
export declare enum ConnectionStateType {
    NONE = 0,
    INITIALIZED = 1,
    CONNECTING = 2,
    CONNECTED = 3,
    RECONNECTING = 4,
    DISCONNECTED = 5,
    LOGOUT = 6
}
